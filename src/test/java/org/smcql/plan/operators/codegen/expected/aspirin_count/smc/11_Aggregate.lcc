package org.smcql.generated.aspirin_count.Aggregate11;


// if used for COUNT(DISTINCT), distinct inserted at compile time
int64[1] run@v(int64[v] a, int64[v] b) {
	int tupleCount;
	getArrElements(a, tupleCount);	

    secure int64[1] dst;
	dst[0] = tupleCount;

	int dstIdx = 1;
	setArrElements(dst, dstIdx);
	return dst;
}
